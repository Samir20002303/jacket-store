## Supabase Setup

1. Open Supabase SQL Editor for your `jacket-store` project.
2. Run `supabase/schema.sql`.
3. Run `supabase/seed.sql`.
4. Start Next.js app with `.env.local` configured.

The app uses:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
